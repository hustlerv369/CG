fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0
pytest>=8.0
httpx>=0.27           # required by FastAPI TestClient

# Optional — only needed for {{web:URL}} placeholders and the upcoming
# 'browser' agent type. Lazy-imported in dashboard.py; dashboard runs
# without it but web placeholders return an install-instructions error.
# After install, also run: playwright install chromium
playwright>=1.45
